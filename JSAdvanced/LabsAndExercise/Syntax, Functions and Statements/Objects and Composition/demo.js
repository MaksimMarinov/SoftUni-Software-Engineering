function solve(input) {
    let product = 'orange';
    let city = 'Sofia';
    let price = 1;
let obj={
    productInfo: {
        city: city,
        price: price
    }
}
if(obj.productInfo.hasOwnProperty('city')){
    console.log('yes');
}
 ccc
console.log(typeof product);
}solve(['Isacc / 25 / Apple, GravityGun',
'Derek / 12 / BarrelVest, DestructionSword',
'Hes / 1 / Desolator, Sentinel, Antara']
)